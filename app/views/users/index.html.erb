<h1>User Lists</h1>

<table style="width: 20%">
  <thead>
    <th>Email</th>
    <th>Admin?</th>
    <th>Edit</th>
  </thead>
  <tbody>
<% @users.each do |user| %>
  <tr>
    <td><%= user.email %></td>
    <td><%= user.admin %></td>
    <td><%= link_to "Edit", edit_admin_user_path(current_user, user.id) %></td>
  </tr>
<% end %>
  </tbody>
</table>
